naftiko: 1.0.0-alpha2
info:
  label: Pure1 Public REST API — Pod Replica Links
  description: 'Pure1 Public REST API — Pod Replica Links. 1 operations. Lead operation: Pure Storage Get Pod Replica Links.
    Self-contained Naftiko capability covering one Pure Storage business surface.'
  tags:
  - Pure Storage
  - Pod Replica Links
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PURE_STORAGE_API_KEY: PURE_STORAGE_API_KEY
capability:
  consumes:
  - type: http
    namespace: pure1-cloud-pod-replica-links
    baseUri: https://api-staging.pure1.purestorage.com
    description: Pure1 Public REST API — Pod Replica Links business capability. Self-contained, no shared references.
    resources:
    - name: api-1.5-pod-replica-links
      path: /api/1.5/pod-replica-links
      operations:
      - name: get
        method: GET
        description: Pure Storage Get Pod Replica Links
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.PURE_STORAGE_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: pure1-cloud-pod-replica-links-rest
    port: 8080
    description: REST adapter for Pure1 Public REST API — Pod Replica Links. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/api/1-5/pod-replica-links
      name: api-1-5-pod-replica-links
      description: REST surface for api-1.5-pod-replica-links.
      operations:
      - method: GET
        name: get
        description: Pure Storage Get Pod Replica Links
        call: pure1-cloud-pod-replica-links.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: pure1-cloud-pod-replica-links-mcp
    port: 9090
    transport: http
    description: MCP adapter for Pure1 Public REST API — Pod Replica Links. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: pure-storage-get-pod-replica
      description: Pure Storage Get Pod Replica Links
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: pure1-cloud-pod-replica-links.get
      outputParameters:
      - type: object
        mapping: $.
