naftiko: 1.0.0-alpha2
info:
  label: FlashBlade REST API — OIDC SSO
  description: 'FlashBlade REST API — OIDC SSO. 4 operations. Lead operation: Pure Storage List OIDC SSO Configurations. Self-contained
    Naftiko capability covering one Pure Storage business surface.'
  tags:
  - Pure Storage
  - OIDC SSO
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PURE_STORAGE_API_KEY: PURE_STORAGE_API_KEY
capability:
  consumes:
  - type: http
    namespace: flashblade-rest-oidc-sso
    baseUri: http://[array]
    description: FlashBlade REST API — OIDC SSO business capability. Self-contained, no shared references.
    resources:
    - name: api-2.26-sso-oidc-idps
      path: /api/2.26/sso/oidc/idps
      operations:
      - name: get
        method: GET
        description: Pure Storage List OIDC SSO Configurations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: post
        method: POST
        description: Pure Storage Create OIDC SSO Configuration
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: Pure Storage Delete OIDC SSO Configurations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: patch
        method: PATCH
        description: Pure Storage Modify OIDC SSO Configuration
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: apikey
      key: x-auth-token
      value: '{{env.PURE_STORAGE_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: flashblade-rest-oidc-sso-rest
    port: 8080
    description: REST adapter for FlashBlade REST API — OIDC SSO. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/2-26/sso/oidc/idps
      name: api-2-26-sso-oidc-idps
      description: REST surface for api-2.26-sso-oidc-idps.
      operations:
      - method: GET
        name: get
        description: Pure Storage List OIDC SSO Configurations
        call: flashblade-rest-oidc-sso.get
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: post
        description: Pure Storage Create OIDC SSO Configuration
        call: flashblade-rest-oidc-sso.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: Pure Storage Delete OIDC SSO Configurations
        call: flashblade-rest-oidc-sso.delete
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: Pure Storage Modify OIDC SSO Configuration
        call: flashblade-rest-oidc-sso.patch
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: flashblade-rest-oidc-sso-mcp
    port: 9090
    transport: http
    description: MCP adapter for FlashBlade REST API — OIDC SSO. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: pure-storage-list-oidc-sso
      description: Pure Storage List OIDC SSO Configurations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: flashblade-rest-oidc-sso.get
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-create-oidc-sso
      description: Pure Storage Create OIDC SSO Configuration
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: flashblade-rest-oidc-sso.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-delete-oidc-sso
      description: Pure Storage Delete OIDC SSO Configurations
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: flashblade-rest-oidc-sso.delete
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-modify-oidc-sso
      description: Pure Storage Modify OIDC SSO Configuration
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: flashblade-rest-oidc-sso.patch
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
