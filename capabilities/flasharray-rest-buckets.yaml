naftiko: 1.0.0-alpha2
info:
  label: FlashArray REST API — Buckets
  description: 'FlashArray REST API — Buckets. 6 operations. Lead operation: Pure Storage GET Buckets. Self-contained Naftiko
    capability covering one Pure Storage business surface.'
  tags:
  - Pure Storage
  - Buckets
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PURE_STORAGE_API_KEY: PURE_STORAGE_API_KEY
capability:
  consumes:
  - type: http
    namespace: flasharray-rest-buckets
    baseUri: ''
    description: FlashArray REST API — Buckets business capability. Self-contained, no shared references.
    resources:
    - name: api-2.52-buckets
      path: /api/2.52/buckets
      operations:
      - name: get
        method: GET
        description: Pure Storage GET Buckets
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: post
        method: POST
        description: Pure Storage POST Buckets
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: Pure Storage DELETE Buckets
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: patch
        method: PATCH
        description: Pure Storage PATCH Buckets
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: api-2.52-buckets-performance
      path: /api/2.52/buckets/performance
      operations:
      - name: get
        method: GET
        description: Pure Storage List Bucket Performance Data
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: api-2.52-buckets-space
      path: /api/2.52/buckets/space
      operations:
      - name: get
        method: GET
        description: Pure Storage List Bucket Space Information
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: flasharray-rest-buckets-rest
    port: 8080
    description: REST adapter for FlashArray REST API — Buckets. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/api/2-52/buckets
      name: api-2-52-buckets
      description: REST surface for api-2.52-buckets.
      operations:
      - method: GET
        name: get
        description: Pure Storage GET Buckets
        call: flasharray-rest-buckets.get
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: post
        description: Pure Storage POST Buckets
        call: flasharray-rest-buckets.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: Pure Storage DELETE Buckets
        call: flasharray-rest-buckets.delete
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: Pure Storage PATCH Buckets
        call: flasharray-rest-buckets.patch
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/2-52/buckets/performance
      name: api-2-52-buckets-performance
      description: REST surface for api-2.52-buckets-performance.
      operations:
      - method: GET
        name: get
        description: Pure Storage List Bucket Performance Data
        call: flasharray-rest-buckets.get
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/2-52/buckets/space
      name: api-2-52-buckets-space
      description: REST surface for api-2.52-buckets-space.
      operations:
      - method: GET
        name: get
        description: Pure Storage List Bucket Space Information
        call: flasharray-rest-buckets.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: flasharray-rest-buckets-mcp
    port: 9090
    transport: http
    description: MCP adapter for FlashArray REST API — Buckets. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: pure-storage-get-buckets
      description: Pure Storage GET Buckets
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: flasharray-rest-buckets.get
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-post-buckets
      description: Pure Storage POST Buckets
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: flasharray-rest-buckets.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-delete-buckets
      description: Pure Storage DELETE Buckets
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: flasharray-rest-buckets.delete
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-patch-buckets
      description: Pure Storage PATCH Buckets
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: flasharray-rest-buckets.patch
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-list-bucket-performance
      description: Pure Storage List Bucket Performance Data
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: flasharray-rest-buckets.get
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-list-bucket-space
      description: Pure Storage List Bucket Space Information
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: flasharray-rest-buckets.get
      outputParameters:
      - type: object
        mapping: $.
