naftiko: 1.0.0-alpha2
info:
  label: FlashBlade REST API — Clients
  description: 'FlashBlade REST API — Clients. 2 operations. Lead operation: Pure Storage GET Clients-performance. Self-contained
    Naftiko capability covering one Pure Storage business surface.'
  tags:
  - Pure Storage
  - Clients
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PURE_STORAGE_API_KEY: PURE_STORAGE_API_KEY
capability:
  consumes:
  - type: http
    namespace: flashblade-rest-clients
    baseUri: http://[array]
    description: FlashBlade REST API — Clients business capability. Self-contained, no shared references.
    resources:
    - name: api-2.26-arrays-clients-performance
      path: /api/2.26/arrays/clients/performance
      operations:
      - name: get
        method: GET
        description: Pure Storage GET Clients-performance
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: api-2.26-arrays-clients-s3-specific-performance
      path: /api/2.26/arrays/clients/s3-specific-performance
      operations:
      - name: get
        method: GET
        description: Pure Storage GET Clients-performance
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: x-auth-token
      value: '{{env.PURE_STORAGE_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: flashblade-rest-clients-rest
    port: 8080
    description: REST adapter for FlashBlade REST API — Clients. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/api/2-26/arrays/clients/performance
      name: api-2-26-arrays-clients-performance
      description: REST surface for api-2.26-arrays-clients-performance.
      operations:
      - method: GET
        name: get
        description: Pure Storage GET Clients-performance
        call: flashblade-rest-clients.get
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/2-26/arrays/clients/s3-specific-performance
      name: api-2-26-arrays-clients-s3-specific-performance
      description: REST surface for api-2.26-arrays-clients-s3-specific-performance.
      operations:
      - method: GET
        name: get
        description: Pure Storage GET Clients-performance
        call: flashblade-rest-clients.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: flashblade-rest-clients-mcp
    port: 9090
    transport: http
    description: MCP adapter for FlashBlade REST API — Clients. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: pure-storage-get-clients-performance
      description: Pure Storage GET Clients-performance
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: flashblade-rest-clients.get
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-get-clients-performance-2
      description: Pure Storage GET Clients-performance
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: flashblade-rest-clients.get
      outputParameters:
      - type: object
        mapping: $.
