naftiko: 1.0.0-alpha2
info:
  label: FlashArray REST API — Servers
  description: 'FlashArray REST API — Servers. 4 operations. Lead operation: Pure Storage List Servers. Self-contained Naftiko
    capability covering one Pure Storage business surface.'
  tags:
  - Pure Storage
  - Servers
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PURE_STORAGE_API_KEY: PURE_STORAGE_API_KEY
capability:
  consumes:
  - type: http
    namespace: flasharray-rest-servers
    baseUri: ''
    description: FlashArray REST API — Servers business capability. Self-contained, no shared references.
    resources:
    - name: api-2.52-servers
      path: /api/2.52/servers
      operations:
      - name: get
        method: GET
        description: Pure Storage List Servers
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: post
        method: POST
        description: Pure Storage Create Server
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
      - name: delete
        method: DELETE
        description: Pure Storage Delete Server
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: patch
        method: PATCH
        description: Pure Storage Modify Server
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: flasharray-rest-servers-rest
    port: 8080
    description: REST adapter for FlashArray REST API — Servers. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/api/2-52/servers
      name: api-2-52-servers
      description: REST surface for api-2.52-servers.
      operations:
      - method: GET
        name: get
        description: Pure Storage List Servers
        call: flasharray-rest-servers.get
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: post
        description: Pure Storage Create Server
        call: flasharray-rest-servers.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: Pure Storage Delete Server
        call: flasharray-rest-servers.delete
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: Pure Storage Modify Server
        call: flasharray-rest-servers.patch
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: flasharray-rest-servers-mcp
    port: 9090
    transport: http
    description: MCP adapter for FlashArray REST API — Servers. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: pure-storage-list-servers
      description: Pure Storage List Servers
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: flasharray-rest-servers.get
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-create-server
      description: Pure Storage Create Server
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: flasharray-rest-servers.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-delete-server
      description: Pure Storage Delete Server
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: flasharray-rest-servers.delete
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-modify-server
      description: Pure Storage Modify Server
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: flasharray-rest-servers.patch
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
