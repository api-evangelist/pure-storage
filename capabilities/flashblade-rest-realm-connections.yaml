naftiko: 1.0.0-alpha2
info:
  label: FlashBlade REST API — Realm Connections
  description: 'FlashBlade REST API — Realm Connections. 6 operations. Lead operation: Pure Storage GET Realm-connections.
    Self-contained Naftiko capability covering one Pure Storage business surface.'
  tags:
  - Pure Storage
  - Realm Connections
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PURE_STORAGE_API_KEY: PURE_STORAGE_API_KEY
capability:
  consumes:
  - type: http
    namespace: flashblade-rest-realm-connections
    baseUri: http://[array]
    description: FlashBlade REST API — Realm Connections business capability. Self-contained, no shared references.
    resources:
    - name: api-2.26-realm-connections
      path: /api/2.26/realm-connections
      operations:
      - name: get
        method: GET
        description: Pure Storage GET Realm-connections
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: post
        method: POST
        description: Pure Storage POST Realm-connections
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: Pure Storage DELETE Realm-connections
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: api-2.26-realm-connections-connection-key
      path: /api/2.26/realm-connections/connection-key
      operations:
      - name: get
        method: GET
        description: Pure Storage GET Realm-connections/connection-key
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: post
        method: POST
        description: Pure Storage POST Realm-connections/connection-key
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: delete
        method: DELETE
        description: Pure Storage DELETE Realm-connections/connection-key
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: x-auth-token
      value: '{{env.PURE_STORAGE_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: flashblade-rest-realm-connections-rest
    port: 8080
    description: REST adapter for FlashBlade REST API — Realm Connections. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/2-26/realm-connections
      name: api-2-26-realm-connections
      description: REST surface for api-2.26-realm-connections.
      operations:
      - method: GET
        name: get
        description: Pure Storage GET Realm-connections
        call: flashblade-rest-realm-connections.get
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: post
        description: Pure Storage POST Realm-connections
        call: flashblade-rest-realm-connections.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: Pure Storage DELETE Realm-connections
        call: flashblade-rest-realm-connections.delete
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/2-26/realm-connections/connection-key
      name: api-2-26-realm-connections-connection-key
      description: REST surface for api-2.26-realm-connections-connection-key.
      operations:
      - method: GET
        name: get
        description: Pure Storage GET Realm-connections/connection-key
        call: flashblade-rest-realm-connections.get
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: post
        description: Pure Storage POST Realm-connections/connection-key
        call: flashblade-rest-realm-connections.post
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: Pure Storage DELETE Realm-connections/connection-key
        call: flashblade-rest-realm-connections.delete
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: flashblade-rest-realm-connections-mcp
    port: 9090
    transport: http
    description: MCP adapter for FlashBlade REST API — Realm Connections. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: pure-storage-get-realm-connections
      description: Pure Storage GET Realm-connections
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: flashblade-rest-realm-connections.get
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-post-realm-connections
      description: Pure Storage POST Realm-connections
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: flashblade-rest-realm-connections.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-delete-realm-connections
      description: Pure Storage DELETE Realm-connections
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: flashblade-rest-realm-connections.delete
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-get-realm-connections-2
      description: Pure Storage GET Realm-connections/connection-key
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: flashblade-rest-realm-connections.get
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-post-realm-connections-2
      description: Pure Storage POST Realm-connections/connection-key
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: flashblade-rest-realm-connections.post
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-delete-realm-connections-2
      description: Pure Storage DELETE Realm-connections/connection-key
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: flashblade-rest-realm-connections.delete
      outputParameters:
      - type: object
        mapping: $.
