naftiko: 1.0.0-alpha2
info:
  label: FlashArray REST API — Files
  description: 'FlashArray REST API — Files. 1 operations. Lead operation: Pure Storage Create a File Copy. Self-contained
    Naftiko capability covering one Pure Storage business surface.'
  tags:
  - Pure Storage
  - Files
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PURE_STORAGE_API_KEY: PURE_STORAGE_API_KEY
capability:
  consumes:
  - type: http
    namespace: flasharray-rest-files
    baseUri: ''
    description: FlashArray REST API — Files business capability. Self-contained, no shared references.
    resources:
    - name: api-2.52-files
      path: /api/2.52/files
      operations:
      - name: post
        method: POST
        description: Pure Storage Create a File Copy
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: flasharray-rest-files-rest
    port: 8080
    description: REST adapter for FlashArray REST API — Files. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/api/2-52/files
      name: api-2-52-files
      description: REST surface for api-2.52-files.
      operations:
      - method: POST
        name: post
        description: Pure Storage Create a File Copy
        call: flasharray-rest-files.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: flasharray-rest-files-mcp
    port: 9090
    transport: http
    description: MCP adapter for FlashArray REST API — Files. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: pure-storage-create-file-copy
      description: Pure Storage Create a File Copy
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: flasharray-rest-files.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
