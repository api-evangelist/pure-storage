naftiko: 1.0.0-alpha2
info:
  label: FlashBlade REST API — Maintenance Windows
  description: 'FlashBlade REST API — Maintenance Windows. 3 operations. Lead operation: Pure Storage List Maintenance Window
    Details. Self-contained Naftiko capability covering one Pure Storage business surface.'
  tags:
  - Pure Storage
  - Maintenance Windows
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PURE_STORAGE_API_KEY: PURE_STORAGE_API_KEY
capability:
  consumes:
  - type: http
    namespace: flashblade-rest-maintenance-windows
    baseUri: http://[array]
    description: FlashBlade REST API — Maintenance Windows business capability. Self-contained, no shared references.
    resources:
    - name: api-2.26-maintenance-windows
      path: /api/2.26/maintenance-windows
      operations:
      - name: get
        method: GET
        description: Pure Storage List Maintenance Window Details
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: post
        method: POST
        description: Pure Storage Create a Maintenance Window
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: Pure Storage Delete Maintenance Window
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: x-auth-token
      value: '{{env.PURE_STORAGE_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: flashblade-rest-maintenance-windows-rest
    port: 8080
    description: REST adapter for FlashBlade REST API — Maintenance Windows. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/api/2-26/maintenance-windows
      name: api-2-26-maintenance-windows
      description: REST surface for api-2.26-maintenance-windows.
      operations:
      - method: GET
        name: get
        description: Pure Storage List Maintenance Window Details
        call: flashblade-rest-maintenance-windows.get
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: post
        description: Pure Storage Create a Maintenance Window
        call: flashblade-rest-maintenance-windows.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: Pure Storage Delete Maintenance Window
        call: flashblade-rest-maintenance-windows.delete
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: flashblade-rest-maintenance-windows-mcp
    port: 9090
    transport: http
    description: MCP adapter for FlashBlade REST API — Maintenance Windows. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: pure-storage-list-maintenance-window
      description: Pure Storage List Maintenance Window Details
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: flashblade-rest-maintenance-windows.get
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-create-maintenance-window
      description: Pure Storage Create a Maintenance Window
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: flashblade-rest-maintenance-windows.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-delete-maintenance-window
      description: Pure Storage Delete Maintenance Window
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: flashblade-rest-maintenance-windows.delete
      outputParameters:
      - type: object
        mapping: $.
