naftiko: 1.0.0-alpha2
info:
  label: FlashArray REST API — User Group Quotas
  description: 'FlashArray REST API — User Group Quotas. 2 operations. Lead operation: Pure Storage List Group Quotas.. Self-contained
    Naftiko capability covering one Pure Storage business surface.'
  tags:
  - Pure Storage
  - User Group Quotas
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PURE_STORAGE_API_KEY: PURE_STORAGE_API_KEY
capability:
  consumes:
  - type: http
    namespace: flasharray-rest-user-group-quotas
    baseUri: ''
    description: FlashArray REST API — User Group Quotas business capability. Self-contained, no shared references.
    resources:
    - name: api-2.52-directory-group-quotas
      path: /api/2.52/directory-group-quotas
      operations:
      - name: get
        method: GET
        description: Pure Storage List Group Quotas.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: api-2.52-directory-user-quotas
      path: /api/2.52/directory-user-quotas
      operations:
      - name: get
        method: GET
        description: Pure Storage List User Quotas.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: flasharray-rest-user-group-quotas-rest
    port: 8080
    description: REST adapter for FlashArray REST API — User Group Quotas. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/2-52/directory-group-quotas
      name: api-2-52-directory-group-quotas
      description: REST surface for api-2.52-directory-group-quotas.
      operations:
      - method: GET
        name: get
        description: Pure Storage List Group Quotas.
        call: flasharray-rest-user-group-quotas.get
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/2-52/directory-user-quotas
      name: api-2-52-directory-user-quotas
      description: REST surface for api-2.52-directory-user-quotas.
      operations:
      - method: GET
        name: get
        description: Pure Storage List User Quotas.
        call: flasharray-rest-user-group-quotas.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: flasharray-rest-user-group-quotas-mcp
    port: 9090
    transport: http
    description: MCP adapter for FlashArray REST API — User Group Quotas. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: pure-storage-list-group-quotas
      description: Pure Storage List Group Quotas.
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: flasharray-rest-user-group-quotas.get
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-list-user-quotas
      description: Pure Storage List User Quotas.
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: flasharray-rest-user-group-quotas.get
      outputParameters:
      - type: object
        mapping: $.
