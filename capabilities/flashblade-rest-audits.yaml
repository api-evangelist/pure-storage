naftiko: 1.0.0-alpha2
info:
  label: FlashBlade REST API — Audits
  description: 'FlashBlade REST API — Audits. 1 operations. Lead operation: Pure Storage GET Audits. Self-contained Naftiko
    capability covering one Pure Storage business surface.'
  tags:
  - Pure Storage
  - Audits
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PURE_STORAGE_API_KEY: PURE_STORAGE_API_KEY
capability:
  consumes:
  - type: http
    namespace: flashblade-rest-audits
    baseUri: http://[array]
    description: FlashBlade REST API — Audits business capability. Self-contained, no shared references.
    resources:
    - name: api-2.26-audits
      path: /api/2.26/audits
      operations:
      - name: get
        method: GET
        description: Pure Storage GET Audits
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: x-auth-token
      value: '{{env.PURE_STORAGE_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: flashblade-rest-audits-rest
    port: 8080
    description: REST adapter for FlashBlade REST API — Audits. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/api/2-26/audits
      name: api-2-26-audits
      description: REST surface for api-2.26-audits.
      operations:
      - method: GET
        name: get
        description: Pure Storage GET Audits
        call: flashblade-rest-audits.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: flashblade-rest-audits-mcp
    port: 9090
    transport: http
    description: MCP adapter for FlashBlade REST API — Audits. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: pure-storage-get-audits
      description: Pure Storage GET Audits
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: flashblade-rest-audits.get
      outputParameters:
      - type: object
        mapping: $.
