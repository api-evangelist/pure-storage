naftiko: 1.0.0-alpha2
info:
  label: Pure1 Public REST API — Audits
  description: 'Pure1 Public REST API — Audits. 1 operations. Lead operation: Pure Storage Get Audits. Self-contained Naftiko
    capability covering one Pure Storage business surface.'
  tags:
  - Pure Storage
  - Audits
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PURE_STORAGE_API_KEY: PURE_STORAGE_API_KEY
capability:
  consumes:
  - type: http
    namespace: pure1-cloud-audits
    baseUri: https://api-staging.pure1.purestorage.com
    description: Pure1 Public REST API — Audits business capability. Self-contained, no shared references.
    resources:
    - name: api-1.5-audits
      path: /api/1.5/audits
      operations:
      - name: get
        method: GET
        description: Pure Storage Get Audits
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.PURE_STORAGE_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: pure1-cloud-audits-rest
    port: 8080
    description: REST adapter for Pure1 Public REST API — Audits. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/1-5/audits
      name: api-1-5-audits
      description: REST surface for api-1.5-audits.
      operations:
      - method: GET
        name: get
        description: Pure Storage Get Audits
        call: pure1-cloud-audits.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: pure1-cloud-audits-mcp
    port: 9090
    transport: http
    description: MCP adapter for Pure1 Public REST API — Audits. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: pure-storage-get-audits
      description: Pure Storage Get Audits
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: pure1-cloud-audits.get
      outputParameters:
      - type: object
        mapping: $.
