naftiko: 1.0.0-alpha2
info:
  label: FlashBlade REST API — Resiliency Groups
  description: 'FlashBlade REST API — Resiliency Groups. 2 operations. Lead operation: Pure Storage GET Resiliency-groups.
    Self-contained Naftiko capability covering one Pure Storage business surface.'
  tags:
  - Pure Storage
  - Resiliency Groups
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PURE_STORAGE_API_KEY: PURE_STORAGE_API_KEY
capability:
  consumes:
  - type: http
    namespace: flashblade-rest-resiliency-groups
    baseUri: http://[array]
    description: FlashBlade REST API — Resiliency Groups business capability. Self-contained, no shared references.
    resources:
    - name: api-2.26-resiliency-groups
      path: /api/2.26/resiliency-groups
      operations:
      - name: get
        method: GET
        description: Pure Storage GET Resiliency-groups
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: api-2.26-resiliency-groups-members
      path: /api/2.26/resiliency-groups/members
      operations:
      - name: get
        method: GET
        description: Pure Storage GET Resiliency-groups/members
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: x-auth-token
      value: '{{env.PURE_STORAGE_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: flashblade-rest-resiliency-groups-rest
    port: 8080
    description: REST adapter for FlashBlade REST API — Resiliency Groups. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/2-26/resiliency-groups
      name: api-2-26-resiliency-groups
      description: REST surface for api-2.26-resiliency-groups.
      operations:
      - method: GET
        name: get
        description: Pure Storage GET Resiliency-groups
        call: flashblade-rest-resiliency-groups.get
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/2-26/resiliency-groups/members
      name: api-2-26-resiliency-groups-members
      description: REST surface for api-2.26-resiliency-groups-members.
      operations:
      - method: GET
        name: get
        description: Pure Storage GET Resiliency-groups/members
        call: flashblade-rest-resiliency-groups.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: flashblade-rest-resiliency-groups-mcp
    port: 9090
    transport: http
    description: MCP adapter for FlashBlade REST API — Resiliency Groups. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: pure-storage-get-resiliency-groups
      description: Pure Storage GET Resiliency-groups
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: flashblade-rest-resiliency-groups.get
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-get-resiliency-groups-2
      description: Pure Storage GET Resiliency-groups/members
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: flashblade-rest-resiliency-groups.get
      outputParameters:
      - type: object
        mapping: $.
