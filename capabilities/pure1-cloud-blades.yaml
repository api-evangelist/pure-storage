naftiko: 1.0.0-alpha2
info:
  label: Pure1 Public REST API — Blades
  description: 'Pure1 Public REST API — Blades. 1 operations. Lead operation: Pure Storage Get Blades. Self-contained Naftiko
    capability covering one Pure Storage business surface.'
  tags:
  - Pure Storage
  - Blades
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PURE_STORAGE_API_KEY: PURE_STORAGE_API_KEY
capability:
  consumes:
  - type: http
    namespace: pure1-cloud-blades
    baseUri: https://api-staging.pure1.purestorage.com
    description: Pure1 Public REST API — Blades business capability. Self-contained, no shared references.
    resources:
    - name: api-1.5-blades
      path: /api/1.5/blades
      operations:
      - name: get
        method: GET
        description: Pure Storage Get Blades
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.PURE_STORAGE_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: pure1-cloud-blades-rest
    port: 8080
    description: REST adapter for Pure1 Public REST API — Blades. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/1-5/blades
      name: api-1-5-blades
      description: REST surface for api-1.5-blades.
      operations:
      - method: GET
        name: get
        description: Pure Storage Get Blades
        call: pure1-cloud-blades.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: pure1-cloud-blades-mcp
    port: 9090
    transport: http
    description: MCP adapter for Pure1 Public REST API — Blades. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: pure-storage-get-blades
      description: Pure Storage Get Blades
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: pure1-cloud-blades.get
      outputParameters:
      - type: object
        mapping: $.
