naftiko: 1.0.0-alpha2
info:
  label: Pure1 Public REST API — Subscriptions
  description: 'Pure1 Public REST API — Subscriptions. 3 operations. Lead operation: Pure Storage Get Subscription Assets.
    Self-contained Naftiko capability covering one Pure Storage business surface.'
  tags:
  - Pure Storage
  - Subscriptions
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PURE_STORAGE_API_KEY: PURE_STORAGE_API_KEY
capability:
  consumes:
  - type: http
    namespace: pure1-cloud-subscriptions
    baseUri: https://api-staging.pure1.purestorage.com
    description: Pure1 Public REST API — Subscriptions business capability. Self-contained, no shared references.
    resources:
    - name: api-1.5-subscription-assets
      path: /api/1.5/subscription-assets
      operations:
      - name: get
        method: GET
        description: Pure Storage Get Subscription Assets
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: api-1.5-subscription-licenses
      path: /api/1.5/subscription-licenses
      operations:
      - name: get
        method: GET
        description: Pure Storage Get Subscription Licenses
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: api-1.5-subscriptions
      path: /api/1.5/subscriptions
      operations:
      - name: get
        method: GET
        description: Pure Storage Get Subscriptions
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.PURE_STORAGE_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: pure1-cloud-subscriptions-rest
    port: 8080
    description: REST adapter for Pure1 Public REST API — Subscriptions. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/1-5/subscription-assets
      name: api-1-5-subscription-assets
      description: REST surface for api-1.5-subscription-assets.
      operations:
      - method: GET
        name: get
        description: Pure Storage Get Subscription Assets
        call: pure1-cloud-subscriptions.get
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/1-5/subscription-licenses
      name: api-1-5-subscription-licenses
      description: REST surface for api-1.5-subscription-licenses.
      operations:
      - method: GET
        name: get
        description: Pure Storage Get Subscription Licenses
        call: pure1-cloud-subscriptions.get
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/1-5/subscriptions
      name: api-1-5-subscriptions
      description: REST surface for api-1.5-subscriptions.
      operations:
      - method: GET
        name: get
        description: Pure Storage Get Subscriptions
        call: pure1-cloud-subscriptions.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: pure1-cloud-subscriptions-mcp
    port: 9090
    transport: http
    description: MCP adapter for Pure1 Public REST API — Subscriptions. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: pure-storage-get-subscription-assets
      description: Pure Storage Get Subscription Assets
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: pure1-cloud-subscriptions.get
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-get-subscription-licenses
      description: Pure Storage Get Subscription Licenses
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: pure1-cloud-subscriptions.get
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-get-subscriptions
      description: Pure Storage Get Subscriptions
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: pure1-cloud-subscriptions.get
      outputParameters:
      - type: object
        mapping: $.
