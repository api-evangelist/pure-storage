naftiko: 1.0.0-alpha2
info:
  label: FlashArray REST API — Hardware
  description: 'FlashArray REST API — Hardware. 2 operations. Lead operation: Pure Storage List Hardware Component Information.
    Self-contained Naftiko capability covering one Pure Storage business surface.'
  tags:
  - Pure Storage
  - Hardware
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PURE_STORAGE_API_KEY: PURE_STORAGE_API_KEY
capability:
  consumes:
  - type: http
    namespace: flasharray-rest-hardware
    baseUri: ''
    description: FlashArray REST API — Hardware business capability. Self-contained, no shared references.
    resources:
    - name: api-2.52-hardware
      path: /api/2.52/hardware
      operations:
      - name: get
        method: GET
        description: Pure Storage List Hardware Component Information
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: patch
        method: PATCH
        description: Pure Storage Modify Visual Identification
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: flasharray-rest-hardware-rest
    port: 8080
    description: REST adapter for FlashArray REST API — Hardware. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/2-52/hardware
      name: api-2-52-hardware
      description: REST surface for api-2.52-hardware.
      operations:
      - method: GET
        name: get
        description: Pure Storage List Hardware Component Information
        call: flasharray-rest-hardware.get
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: Pure Storage Modify Visual Identification
        call: flasharray-rest-hardware.patch
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: flasharray-rest-hardware-mcp
    port: 9090
    transport: http
    description: MCP adapter for FlashArray REST API — Hardware. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: pure-storage-list-hardware-component
      description: Pure Storage List Hardware Component Information
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: flasharray-rest-hardware.get
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-modify-visual-identification
      description: Pure Storage Modify Visual Identification
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: flasharray-rest-hardware.patch
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
