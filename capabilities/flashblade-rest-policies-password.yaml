naftiko: 1.0.0-alpha2
info:
  label: FlashBlade REST API — Policies - Password
  description: 'FlashBlade REST API — Policies - Password. 2 operations. Lead operation: Pure Storage List Password Policies.
    Self-contained Naftiko capability covering one Pure Storage business surface.'
  tags:
  - Pure Storage
  - Policies - Password
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PURE_STORAGE_API_KEY: PURE_STORAGE_API_KEY
capability:
  consumes:
  - type: http
    namespace: flashblade-rest-policies-password
    baseUri: http://[array]
    description: FlashBlade REST API — Policies - Password business capability. Self-contained, no shared references.
    resources:
    - name: api-2.26-password-policies
      path: /api/2.26/password-policies
      operations:
      - name: get
        method: GET
        description: Pure Storage List Password Policies
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: patch
        method: PATCH
        description: Pure Storage Modify Password Policies
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: apikey
      key: x-auth-token
      value: '{{env.PURE_STORAGE_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: flashblade-rest-policies-password-rest
    port: 8080
    description: REST adapter for FlashBlade REST API — Policies - Password. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/api/2-26/password-policies
      name: api-2-26-password-policies
      description: REST surface for api-2.26-password-policies.
      operations:
      - method: GET
        name: get
        description: Pure Storage List Password Policies
        call: flashblade-rest-policies-password.get
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: Pure Storage Modify Password Policies
        call: flashblade-rest-policies-password.patch
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: flashblade-rest-policies-password-mcp
    port: 9090
    transport: http
    description: MCP adapter for FlashBlade REST API — Policies - Password. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: pure-storage-list-password-policies
      description: Pure Storage List Password Policies
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: flashblade-rest-policies-password.get
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-modify-password-policies
      description: Pure Storage Modify Password Policies
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: flashblade-rest-policies-password.patch
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
