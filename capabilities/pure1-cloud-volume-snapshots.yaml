naftiko: 1.0.0-alpha2
info:
  label: Pure1 Public REST API — Volume Snapshots
  description: 'Pure1 Public REST API — Volume Snapshots. 1 operations. Lead operation: Pure Storage Get Volume Snapshots.
    Self-contained Naftiko capability covering one Pure Storage business surface.'
  tags:
  - Pure Storage
  - Volume Snapshots
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PURE_STORAGE_API_KEY: PURE_STORAGE_API_KEY
capability:
  consumes:
  - type: http
    namespace: pure1-cloud-volume-snapshots
    baseUri: https://api-staging.pure1.purestorage.com
    description: Pure1 Public REST API — Volume Snapshots business capability. Self-contained, no shared references.
    resources:
    - name: api-1.5-volume-snapshots
      path: /api/1.5/volume-snapshots
      operations:
      - name: get
        method: GET
        description: Pure Storage Get Volume Snapshots
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.PURE_STORAGE_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: pure1-cloud-volume-snapshots-rest
    port: 8080
    description: REST adapter for Pure1 Public REST API — Volume Snapshots. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/1-5/volume-snapshots
      name: api-1-5-volume-snapshots
      description: REST surface for api-1.5-volume-snapshots.
      operations:
      - method: GET
        name: get
        description: Pure Storage Get Volume Snapshots
        call: pure1-cloud-volume-snapshots.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: pure1-cloud-volume-snapshots-mcp
    port: 9090
    transport: http
    description: MCP adapter for Pure1 Public REST API — Volume Snapshots. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: pure-storage-get-volume-snapshots
      description: Pure Storage Get Volume Snapshots
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: pure1-cloud-volume-snapshots.get
      outputParameters:
      - type: object
        mapping: $.
