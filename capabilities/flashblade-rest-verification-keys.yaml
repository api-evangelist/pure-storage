naftiko: 1.0.0-alpha2
info:
  label: FlashBlade REST API — Verification Keys
  description: 'FlashBlade REST API — Verification Keys. 2 operations. Lead operation: Pure Storage GET Verification-keys.
    Self-contained Naftiko capability covering one Pure Storage business surface.'
  tags:
  - Pure Storage
  - Verification Keys
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PURE_STORAGE_API_KEY: PURE_STORAGE_API_KEY
capability:
  consumes:
  - type: http
    namespace: flashblade-rest-verification-keys
    baseUri: http://[array]
    description: FlashBlade REST API — Verification Keys business capability. Self-contained, no shared references.
    resources:
    - name: api-2.26-support-verification-keys
      path: /api/2.26/support/verification-keys
      operations:
      - name: get
        method: GET
        description: Pure Storage GET Verification-keys
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: patch
        method: PATCH
        description: Pure Storage PATCH Verification-keys
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: apikey
      key: x-auth-token
      value: '{{env.PURE_STORAGE_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: flashblade-rest-verification-keys-rest
    port: 8080
    description: REST adapter for FlashBlade REST API — Verification Keys. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/2-26/support/verification-keys
      name: api-2-26-support-verification-keys
      description: REST surface for api-2.26-support-verification-keys.
      operations:
      - method: GET
        name: get
        description: Pure Storage GET Verification-keys
        call: flashblade-rest-verification-keys.get
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: Pure Storage PATCH Verification-keys
        call: flashblade-rest-verification-keys.patch
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: flashblade-rest-verification-keys-mcp
    port: 9090
    transport: http
    description: MCP adapter for FlashBlade REST API — Verification Keys. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: pure-storage-get-verification-keys
      description: Pure Storage GET Verification-keys
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: flashblade-rest-verification-keys.get
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-patch-verification-keys
      description: Pure Storage PATCH Verification-keys
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: flashblade-rest-verification-keys.patch
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
