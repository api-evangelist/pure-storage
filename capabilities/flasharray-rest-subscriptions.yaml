naftiko: 1.0.0-alpha2
info:
  label: FlashArray REST API — Subscriptions
  description: 'FlashArray REST API — Subscriptions. 1 operations. Lead operation: Pure Storage List Subscriptions. Self-contained
    Naftiko capability covering one Pure Storage business surface.'
  tags:
  - Pure Storage
  - Subscriptions
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PURE_STORAGE_API_KEY: PURE_STORAGE_API_KEY
capability:
  consumes:
  - type: http
    namespace: flasharray-rest-subscriptions
    baseUri: ''
    description: FlashArray REST API — Subscriptions business capability. Self-contained, no shared references.
    resources:
    - name: api-2.52-subscriptions
      path: /api/2.52/subscriptions
      operations:
      - name: get
        method: GET
        description: Pure Storage List Subscriptions
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: flasharray-rest-subscriptions-rest
    port: 8080
    description: REST adapter for FlashArray REST API — Subscriptions. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/2-52/subscriptions
      name: api-2-52-subscriptions
      description: REST surface for api-2.52-subscriptions.
      operations:
      - method: GET
        name: get
        description: Pure Storage List Subscriptions
        call: flasharray-rest-subscriptions.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: flasharray-rest-subscriptions-mcp
    port: 9090
    transport: http
    description: MCP adapter for FlashArray REST API — Subscriptions. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: pure-storage-list-subscriptions
      description: Pure Storage List Subscriptions
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: flasharray-rest-subscriptions.get
      outputParameters:
      - type: object
        mapping: $.
