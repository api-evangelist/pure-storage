naftiko: 1.0.0-alpha2
info:
  label: FlashBlade REST API — Bucket Replica Links
  description: 'FlashBlade REST API — Bucket Replica Links. 4 operations. Lead operation: Pure Storage GET Bucket-replica-links.
    Self-contained Naftiko capability covering one Pure Storage business surface.'
  tags:
  - Pure Storage
  - Bucket Replica Links
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PURE_STORAGE_API_KEY: PURE_STORAGE_API_KEY
capability:
  consumes:
  - type: http
    namespace: flashblade-rest-bucket-replica-links
    baseUri: http://[array]
    description: FlashBlade REST API — Bucket Replica Links business capability. Self-contained, no shared references.
    resources:
    - name: api-2.26-bucket-replica-links
      path: /api/2.26/bucket-replica-links
      operations:
      - name: get
        method: GET
        description: Pure Storage GET Bucket-replica-links
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: post
        method: POST
        description: Pure Storage POST Bucket-replica-links
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: Pure Storage DELETE Bucket-replica-links
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: patch
        method: PATCH
        description: Pure Storage PATCH Bucket-replica-links
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: apikey
      key: x-auth-token
      value: '{{env.PURE_STORAGE_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: flashblade-rest-bucket-replica-links-rest
    port: 8080
    description: REST adapter for FlashBlade REST API — Bucket Replica Links. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/api/2-26/bucket-replica-links
      name: api-2-26-bucket-replica-links
      description: REST surface for api-2.26-bucket-replica-links.
      operations:
      - method: GET
        name: get
        description: Pure Storage GET Bucket-replica-links
        call: flashblade-rest-bucket-replica-links.get
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: post
        description: Pure Storage POST Bucket-replica-links
        call: flashblade-rest-bucket-replica-links.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: Pure Storage DELETE Bucket-replica-links
        call: flashblade-rest-bucket-replica-links.delete
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: Pure Storage PATCH Bucket-replica-links
        call: flashblade-rest-bucket-replica-links.patch
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: flashblade-rest-bucket-replica-links-mcp
    port: 9090
    transport: http
    description: MCP adapter for FlashBlade REST API — Bucket Replica Links. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: pure-storage-get-bucket-replica
      description: Pure Storage GET Bucket-replica-links
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: flashblade-rest-bucket-replica-links.get
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-post-bucket-replica
      description: Pure Storage POST Bucket-replica-links
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: flashblade-rest-bucket-replica-links.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-delete-bucket-replica
      description: Pure Storage DELETE Bucket-replica-links
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: flashblade-rest-bucket-replica-links.delete
      outputParameters:
      - type: object
        mapping: $.
    - name: pure-storage-patch-bucket-replica
      description: Pure Storage PATCH Bucket-replica-links
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: flashblade-rest-bucket-replica-links.patch
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
